{
  "name": "memoizer-ts",
  "version": "1.0.0",
  "description": "Allows adding memoization to arbitrary functions. The original function will only be called once for identical sets of arguments, subsequent calls will be returned with memoized results.",
  "homepage": "https://github.com/Chips100/memoizer-ts#readme",
  "bugs":{ 
    "url" : "https://github.com/Chips100/memoizer-ts/issues", 
    "email" : "dennis.janiak@googlemail.com"
  },
  "repository": "github:Chips100/memoizer-ts",
  "scripts": {
    "drop-lib": "rimraf ./lib",
    "build": "npm run drop-lib & tsc --declaration",
    "test": "jest",
    "prepublish": "npm run build"
  },
  "files": ["lib"],
  "author": { 
    "name" : "Dennis Janiak", 
    "email" : "dennis.janiak@googlemail.com",
    "url" : "http://www.dennis-janiak.de/"
  },
  "license": "MIT",
  "main": "./lib/main.js",
  "types": "./lib/main.d.ts",
  "devDependencies": {
    "@types/jest": "^23.1.3",
    "jest": "^23.2.0",
    "rimraf": "^2.6.2",
    "ts-jest": "^22.4.6",
    "typescript": "^2.9.2"
  }
}
